[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ollama-ml-benchmarker"
version = "0.1.0"
description = "A professional benchmarking tool for local Ollama models."
authors = [
    { name = "Your Name", email = "your.email@example.com" }
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "ollama",
    "questionary",
]

[project.scripts]
ollama-benchmark = "ollama_bench:main"

[tool.hatch.build.hooks.custom]
# This is just a placeholder for build configuration if needed
